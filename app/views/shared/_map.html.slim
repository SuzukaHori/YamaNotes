div[data-controller="map" data-map-stations-value="#{Station.all.to_json}"
                          data-map-arrived-ids-value="#{@walk.arrived_stations.map(&:id).to_json}"
                          data-map-current-id-value="#{@walk.current_station.id}"]
  #map style="width: 100%; height: 500px"
